resources:
  jobs:
    meczyki_hourly_ingestion:
      name: meczyki-hourly-ingestion
      tasks:
        - task_key: ingest
          notebook_task:
            notebook_path: ../notebooks/01_ingest
          existing_cluster_id: ${var.cluster_id}